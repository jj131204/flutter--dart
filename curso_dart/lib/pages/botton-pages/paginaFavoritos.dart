import 'package:flutter/material.dart';

class paginaFavoritos extends StatelessWidget {
  const paginaFavoritos({super.key});

  @override
  Widget build(BuildContext context) {
   return Center(
      child: Text("Favoritos", style: TextStyle(fontSize: 20.0),),
    );
  }
}